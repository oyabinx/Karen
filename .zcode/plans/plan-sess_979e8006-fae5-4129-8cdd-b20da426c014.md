# Revisi Realisasi Bulanan + Form Nota

## R1: Realisasi Bulanan — Default List Mobil → Klik → Rincian
- **Controller** (`RealisasiBulananController`): group costs by vehicle; param `?vehicle=X` untuk drill-down
- **View** rewrite (`realisasi-bulanan/index.blade.php`):
  - **Default**: kartu/list mobil yang maintenance bulan itu (nama, plat, total, jumlah maintenance)
  - **Klik mobil** → detail per pos (servis/suku cadang/pelumas/AC) dengan rincian expandable
  - Tombol ← kembali ke list
  - Ringkasan 4 pos tetap di header

## R2: Form Nota — Total dari Rincian (bukan manual)
- **View rewrite** (`costs.blade.php`):
  - Hapus input manual total per pos
  - Total per pos = **auto-sum dari rincian** (readonly display)
  - Tampil "Total (dikalikan koefisien pajak): Rp XXX" — **tanpa menampilkan angka koefisien**
  - Rincian rows tetap bisa add/remove dengan nominal
- **BudgetService::inputNota()**: `raw_amount = sum(details.amount)` per pos (bukan dari input manual)
- **NotaRequest**: hapus validasi manual per pos; validasi hanya pada details (opsional, amount ≥ 0)

## R3: Sinkronisasi
- Update JS di form: auto-sum detail amounts → tampilkan total × koefisien (hidden)
- Pastikan test suite tetap hijau